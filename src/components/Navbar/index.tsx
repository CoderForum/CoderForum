'use client';

import Logo from '../Logo';
import { NavbarContainer } from './styles';

export default function Navbar() {
  return (
    <NavbarContainer>
      <Logo />
    </NavbarContainer>
  );
}
